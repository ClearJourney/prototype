# Public site URL for canonical URLs + sitemap (recommended in production)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Blog admin login password (required)
BLOG_ADMIN_PASSWORD=change-me

# Optional extra signing secret for the admin cookie
# If omitted, we fall back to BLOG_ADMIN_PASSWORD.
BLOG_ADMIN_SECRET=change-me-too

